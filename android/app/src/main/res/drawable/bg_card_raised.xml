<!-- One step brighter than bg_card_lg: nested blocks / inner panels. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/yami_surface_2" />
    <corners android:radius="@dimen/yami_radius_md" />
    <stroke android:width="@dimen/yami_stroke" android:color="@color/yami_divider_soft" />
</shape>
