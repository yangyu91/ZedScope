<!-- Floating hint banner over the WebView (e.g. "capture empty? install the CA"). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/yami_surface_glass" />
    <corners android:radius="@dimen/yami_radius_md" />
    <stroke android:width="@dimen/yami_stroke" android:color="@color/yami_warn_soft" />
</shape>
